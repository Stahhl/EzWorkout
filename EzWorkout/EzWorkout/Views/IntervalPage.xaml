<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:services="clr-namespace:EzWorkout.Services;assembly=EzWorkout"
             xmlns:syncfusion="clr-namespace:Syncfusion.XForms.Pickers;assembly=Syncfusion.SfPicker.XForms"
             xmlns:sfList="clr-namespace:Syncfusion.ListView.XForms;assembly=Syncfusion.SfListView.XForms"
             x:Class="EzWorkout.Views.IntervalPage">

    <ContentPage.Content>
        <StackLayout>
            <StackLayout VerticalOptions="Start">
                <Label Text="ANUS" IsVisible="True"></Label>
            </StackLayout>
            <StackLayout VerticalOptions="CenterAndExpand">
                <Label Text="ANUS" IsVisible="True"></Label>
            </StackLayout>
            <StackLayout HorizontalOptions="Center" VerticalOptions="End" Orientation="Vertical">
                <Label Text="Type: "  />
                <ListView x:Name="typeView" />
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>

    <!--<sfList:SfListView x:Name="listView" 
                           SelectionMode="Single"
                           SelectionGesture="Tap"
                           SelectionBackgroundColor="#E4E4E4"
                           ItemsSource="{Binding Types}"
                           ItemSize="100"
                           DragStartMode="OnHold"
                           IsStickyFooter="True"
                           FooterSize="60"
                           >
    </sfList:SfListView>-->

    <!--<ContentPage.Content>
        <StackLayout VerticalOptions="EndAndExpand" Spacing="20" Padding="15" Orientation="Vertical">
            -->
    <!--<Label Text="Intensity"/>
            <services:EnumBindablePicker x:TypeArguments="services:IntervalIntensity" SelectedItem="{Binding Intensity}"/>-->
    <!--
            <Label Text="Type:"/>
            <services:EnumBindablePicker x:TypeArguments="services:IntervalType" SelectedItem="{Binding Type}" x:Name="typePicker"/>
            -->
    <!--<Grid>
                <syncfusion:SfTimePicker
                    IsVisible="False"
                    ShowHeader="False"
                    x:Name="timePicker"
                    PickerMode="Default" 
                    Time="0:00:00"/>
            </Grid>-->

    <!--<StackLayout VerticalOptions="EndAndExpand" Orientation="Vertical">
                <Button Text="Duration" Clicked="BtnDuration" HorizontalOptions="FillAndExpand" BackgroundColor="LawnGreen"></Button>
                <Button Text="Distance" Clicked="BtnDistance" HorizontalOptions="FillAndExpand" BackgroundColor="LawnGreen"></Button>
                <Button Text="GoTo" Clicked="BtnGoTo" HorizontalOptions="FillAndExpand" BackgroundColor="LawnGreen"></Button>
            </StackLayout>-->
    <!--
            
        </StackLayout>

    </ContentPage.Content>-->

</ContentPage>